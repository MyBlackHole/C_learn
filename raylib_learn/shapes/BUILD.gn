# git clone https://github.com/raysan5/raylib.git
# mkdir build && cd build
# cmake -DSHARED=ON -DSTATIC=ON ..
# make 
# make install
executable("shapes_basic_shapes") {
  cflags = ["-g"]

  ldflags = ["-lraylib"]

  sources = [
    "shapes_basic_shapes.c",
  ]
}
