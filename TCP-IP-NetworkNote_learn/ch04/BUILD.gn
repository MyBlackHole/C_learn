executable("hello_server") {
  cflags = ["-g", "-Wall"]
  sources = [
    "hello_server.c",
  ]
}

executable("hello_client") {
  cflags = ["-g", "-Wall"]
  sources = [
    "hello_client.c",
  ]
}

executable("echo_server") {
  cflags = ["-g", "-Wall"]
  sources = [
    "echo_server.c",
  ]
}

executable("echo_client") {
  cflags = ["-g", "-Wall"]
  sources = [
    "echo_client.c",
  ]
}

