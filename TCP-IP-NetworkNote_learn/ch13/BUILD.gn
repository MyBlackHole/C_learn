executable("oob_send") {
  cflags = ["-g"]
  sources = [
    "oob_send.c",
  ]
}

executable("oob_recv") {
  cflags = ["-g"]
  sources = [
    "oob_recv.c",
  ]
}

executable("peek_recv") {
  cflags = ["-g"]
  sources = [
    "peek_recv.c",
  ]
}

executable("peek_send") {
  cflags = ["-g"]
  sources = [
    "peek_send.c",
  ]
}

