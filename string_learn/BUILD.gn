executable("memmove_test") {
  cflags = ["-g"]

  sources = [
    "memmove_test.c",
  ]
}

executable("memset_test") {
  cflags = ["-g"]

  sources = [
    "memset_test.c",
  ]
  
  include_dirs = ["//include"]

  deps = [
    "//lib:libc",
  ]
}
