executable("test") {
  cflags = ["-g", "-Wall"]
  deps = [":avl_tree"]
  sources = [
    "test.c",
  ]
}

static_library("avl_tree") {
  cflags = ["-g", "-Wall"]
  sources = [
    "avl_tree.c",
    "avl_tree.h",
  ]
}

