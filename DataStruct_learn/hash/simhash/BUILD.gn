executable("test") {
  cflags = ["-g", "-Wall"]
  deps = [":simhash"]
  sources = [
    "test.c",
  ]
}

static_library("simhash") {
  cflags = ["-g", "-Wall"]
  sources = [
    "simhash.h",
    "simhash.c",
  ]
}

