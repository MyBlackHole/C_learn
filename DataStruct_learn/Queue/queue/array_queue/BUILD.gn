executable("test") {
  cflags = ["-g", "-Wall"]
  deps = [":array_queue"]
  sources = [
    "test.c",
  ]
}

static_library("array_queue") {
  cflags = ["-g", "-Wall"]
  sources = [
    "array_queue.c",
    "array_queue.h",
  ]
}

